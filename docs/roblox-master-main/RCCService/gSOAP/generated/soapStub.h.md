# soapStub.h

Source: `roblox-sandbox/RCCService/gSOAP/generated/soapStub.h` (1395 lines)

## Purpose

The central **generated type header**: soapcpp2's serialization-ready C++ mirror of `RCCService.wsdl` ("Generated by gSOAP **2.7.10** from generated/prototypes.h", line 2). Every message element, data type, and operation wrapper the service exchanges is declared here; `soapH.h` adds the function prototypes and `soapC.cpp` the implementations. `RCCServiceSoapServiceImpl.cpp` implements the virtual service methods against these types.

## API

### Enum

```cpp
#define SOAP_TYPE_ns1__LuaType (50)
enum ns1__LuaType { ns1__LuaType__LUA_USCORETNIL = 0, ..._LUA_USCORETBOOLEAN = 1,
                    ..._LUA_USCORETNUMBER = 2, ..._LUA_USCORETSTRING = 3, ..._LUA_USCORETTABLE = 4 };
```

(`USCORE` = underscore encoding of `LUA_TNIL` etc.)

### Data classes (`ns1__*`, lines 41–173) — each with `struct soap *soap` transient member, virtual `soap_type()/soap_default()/soap_serialize()/soap_put()/soap_out()/soap_get()/soap_in()`

| Class | SOAP_TYPE | Members |
| --- | --- | --- |
| `ns1__Status` | 8 | `std::string *version`, `int environmentCount` |
| `ns1__Job` | 9 | `std::string id`, `double expirationInSeconds`, `int category`, `double cores` |
| `ns1__ScriptExecution` | 10 | `std::string *name`, `*script`, `ns1__ArrayOfLuaValue *arguments` |
| `ns1__ArrayOfLuaValue` | 11 | `std::vector<ns1__LuaValue *> LuaValue` |
| `ns1__ArrayOfJob` | 12 | `std::vector<ns1__Job *> Job` |
| `ns1__LuaValue` | 13 | `enum ns1__LuaType type`, `std::string *value`, `ns1__ArrayOfLuaValue *table` |

Optional (pointer) vs required (value) members follow the WSDL's minOccurs.

### Message-element classes `_ns1__<Op>` / `_ns1__<Op>Response` (lines 175–930, SOAP_TYPEs 14–49)

One request+response pair per operation (18 ops: HelloWorld … DiagEx), matching the WSDL table exactly — e.g. `_ns1__OpenJob { ns1__Job *job; ns1__ScriptExecution *script; }`, `_ns1__OpenJobResponse { std::vector<ns1__LuaValue *> OpenJobResult; }`, `_ns1__RenewLeaseResponse { double RenewLeaseResult; }`. Response result members carry the comment "SOAP 1.2 RPC return element".

### Operation wrappers (lines 932–1290)

- `struct __ns2__<Op>` ×18 (SOAP_TYPEs 66–134): wrappers for the **SOAP 1.1** binding (`ns2 = http://roblox.com/RCCServiceSoap`), each holding one `_ns1__<Op>*`.
- `struct __ns3__<Op>` ×18 (SOAP_TYPEs 136–170): same for the **SOAP 1.2** binding (`ns3`).

### SOAP envelope machinery (lines 1292–1353)

`SOAP_ENV__Header` (empty struct; `char dummy` only under `WITH_NOEMPTYSTRUCT`), `SOAP_ENV__Code` (Value + recursive Subcode), `SOAP_ENV__Detail` (`int __type; void *fault; char *__any`), `SOAP_ENV__Reason` (`SOAP_ENV__Text`) and a combined `SOAP_ENV__Fault` carrying both SOAP 1.1 fields (`faultcode/faultstring/faultactor/detail`) and 1.2 fields (`SOAP_ENV__Code/Reason/Node/Role/Detail`) so one struct serves both bindings.

### Base typedefs

`typedef char *_QName` (1370), `typedef char *_XML` (1375).

## Usage

Consumed by everything in `generated/` plus `RCCServiceSoapServiceImpl.cpp`; regenerated wholesale by generate.bat — never hand-edit.

## Gotchas

- The two `#if 0 /* volatile type: do not redeclare here */` blocks near the top are where wsdl2h would have placed types redirected to imports.
- All classes use raw pointers for optional elements — ownership belongs to the soap context (`soap_destroy` frees them); leaking is easy if objects are built outside a context.
- `ns1__Job::id` is a value `std::string` but `ns1__Status::version` is a pointer — required-vs-optional, not inconsistency.
- SOAP_TYPE ids are stable per generation run and used by `soap_dom_element::type` dispatch and serializer tables.

UNKNOWN: none material — file is fully generated and self-describing.

