# wsa3.h

Source: `roblox-sandbox/RCCService/gSOAP/import/wsa3.h` (176 lines)

## Purpose

gSOAP import stub for **WS-Addressing 2003/03** (`http://schemas.xmlsoap.org/ws/2003/03/addressing`) — the earliest WS-Addressing revision in the vendored set. Generated by `wsdl2h -cgye`, then modified by Robert van Engelen to import (not copy) the namespace and to add `#define SOAP_WSA_2003` so the wsaapi plugin can version-dispatch. Vendored toolchain material; not used by RCCService's WSDL.

## API

- `#define SOAP_WSA_2003` (line 21) — compile-time marker for plugin code.
- Namespace directives (lines 44–46): prefix `wsa3`, elementForm qualified, attributeForm unqualified. (Note the stale comment at line 14 says "wsa4" — generation artifact.)
- Enum: `wsa3__RelationshipTypeValues { wsa3__Response }` + typedef synonym (lines 75–80).
- Structs:
  - `wsa3__EndpointReferenceType` (lines 83–106): required `char* Address`; optional `ReferenceProperties*`, `_QName* PortType`, `ServiceNameType* ServiceName`; extensibility via `int __size; _XML *__any;` and attribute `@_XML __anyAttribute`.
  - `wsa3__ReferencePropertiesType` (109–119): pure wildcard bag (`__size` / `_XML *__any`).
  - `wsa3__ServiceNameType` (122–133): simpleContent `_QName __item` + optional attribute `@char* PortName`.
  - `wsa3__Relationship` (136–147): simpleContent URI + optional `@_QName RelationshipType`.
- Element typedefs (lines 150–174): `_wsa3__EndpointReference`, `_wsa3__MessageID`, `_wsa3__RelatesTo`, `_wsa3__To`, `_wsa3__Action`, `_wsa3__From`, `_wsa3__ReplyTo`, `_wsa3__FaultTo`, `_wsa3__Recipient`.

## Usage

```
#import "wsa3.h"        // with the wsaapi plugin for header processing
soapcpp2 -Iimport ...
```

## Gotchas

- Field-name digits (`Address 1;` etc.) are gSOAP occurrence markers, not syntax errors — they are stripped by soapcpp2.
- Revision trap: four wsa stubs coexist (2003/03, 2004/03, 2004/08, 2005/03); mixing prefixes across a spec silently changes message headers.
- Not referenced anywhere in RCCService.

UNKNOWN: none beyond the above.

